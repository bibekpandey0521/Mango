@model IEnumerable<CouponDto>
@* 
<div class="container py-5">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-dark py-4 rounded-top-4">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h2 class="text-white fw-bold mb-0">
                        Coupons List
                    </h2>
                    <p class="text-secondary mb-0 mt-1 small">Manage all your discount coupons</p>
                </div>
                <div class="col-md-6 text-md-end mt-3 mt-md-0">
                    <a asp-action="CouponCreate" class="btn btn-warning fw-semibold px-4 rounded-pill shadow-sm">
                        <i class="bi bi-plus-circle-fill me-2"></i>Create New Coupon
                    </a>
                </div>
            </div>
        </div>

        <div class="card-body p-4">
            @if (!Model.Any())
            {
                <div class="text-center py-5">
                    <i class="bi bi-inbox display-1 text-muted"></i>
                    <p class="mt-3 text-muted fs-5">No coupons found. Create one to get started!</p>
                </div>
            }
            else
            {
                <div class="table-responsive rounded-3 border">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-dark">
                            <tr>
                                <th class="py-3 px-4">
                                    Coupon Code
                                </th>
                                <th class="py-3 px-4">
                                     Discount Amount
                                </th>
                                <th class="py-3 px-4">
                                     Minimum Amount
                                </th>
                                <th class="py-3 px-4 text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="px-4">
                                        <span class="badge bg-info text-dark fs-6 px-3 py-2 rounded-pill fw-bold">
                                            @item.CouponCode
                                        </span>
                                    </td>
                                    <td class="px-4">
                                        <span class="text-success fw-semibold fs-6">
                                            
                                            @((item.DiscountAmount).ToString("c"))
                                        </span>
                                    </td>
                                    <td class="px-4">
                                        <span class="text-primary fw-semibold fs-6">
                                            @item.MinAmount
                                            ((item.MinAmount).ToString("c")) 
                                        </span>
                                    </td>
                                    <td class="px-4 text-center">
                                        <a asp-action="CouponDelete"
                                           asp-route-couponId="@item.CouponId"
                                           class="btn btn-outline-danger btn-sm rounded-pill px-3"
                                           onclick="return confirm('Are you sure you want to delete this coupon?')">
                                            <i class="bi bi-trash3-fill me-1"></i> Delete
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <div class="d-flex justify-content-between align-items-center mt-3 px-1">
                    <small class="text-muted">
                        <i class="bi bi-info-circle me-1"></i>
                        Showing <strong>@Model.Count()</strong> coupon(s)
                    </small>
                </div>
            }
        </div>
    </div>
</div> *@



<div class="card shadow border-0 mt-4">
	<div class="card-header bg-secondary bg-gradient ml-0 py-3">
		<div class="row">
			<div class="col-12 text-center">
				<h1 class="text-white">Coupons List</h1>
			</div>
		</div>
	</div>
	<div class="card-body p-4">
		<div class="row pb-3">
			<div class="col-6">
			</div>
			<div class="col-6 text-end">
				<a asp-action="CouponCreate" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Create New Coupon</a>

			</div>
		</div>
		<table class="table">
			<thead>
				<tr>
					<th>
						Coupon Code
					</th>
					<th>
						Discount Amount
					</th>
					<th>
						Minimum Amount
					</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				@foreach(var item in Model)
				{
				    <tr>
					    <td> @item.CouponCode
					    </td>
					    <td> @((item.DiscountAmount).ToString("c"))
					    </td>
						    <td>
							    @((item.MinAmount).ToString("c"))
					    </td>
					    <td>
							    <a asp-action="CouponDelete" asp-route-couponId="@item.CouponId" class="btn btn-danger">
							    <i class="bi bi-trash"></i>
						    </a>
					    </td>
				    </tr>
				}
			</tbody>
		</table>

	</div>

</div>